'use strict';

var streams = [
    {
        path: '/stream/newest',
        alias: '/',
        title: '最新影片'
    },
    {
        path: '/stream/reported',
        title: '新聞報導'
    }
];

export default streams;
