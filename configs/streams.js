'use strict';

var streams = {
    newest: {
        path: '/stream/newest',
        alias: '/',
        title: '最新影片'
    },
    reported: {
        path: '/stream/reported',
        title: '新聞報導'
    }
};

export default streams;
