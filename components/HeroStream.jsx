'use strict';

var React = require('react');

var HeroStream = React.createClass({
    render: function() {
        return (
            <div>Hero</div>
        );
    }
});

module.exports = HeroStream;
